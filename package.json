{
  "name": "route2-showcase",
  "version": "1.0.0",
  "description": "testing experimental network stubbing",
  "scripts": {
    "start": "npx ./node_modules/.bin/json-server --static . data.json -m middleware.js --config json-server.json",
    "cy:open": "npx cypress open"
  },
  "devDependencies": {
    "json-server": "0.15.1",
    "sendmail": "^1.6.1",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@bahmutov/cly": "^1.3.1"
  }
}
